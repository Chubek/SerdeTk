# Bash completions

Source the file, or copy it into the system completion directory:

```sh
# per-shell
source satie.bash

# system-wide
cp satie.bash /etc/bash_completion.d/satie
```

Provides `--engine`, `--model`, and file completion for `satie-cli`.
