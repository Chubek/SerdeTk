# Fish completions

Copy `satie.fish` to `~/.config/fish/completions/`:

```sh
cp satie.fish ~/.config/fish/completions/
```

Provides `--engine`, `--model`, and file-path completion for `satie-cli`.
