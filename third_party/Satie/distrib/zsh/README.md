# Zsh completions

Copy `_satie` to a directory in `$fpath`, e.g.:

```sh
cp _satie /usr/local/share/zsh/site-functions/
exec zsh
```

Provides engine choice and file completion for `satie-cli` / `satie-repl`.
